Read rangers/constitution.prompt, then read every file it refers to recursively, and obey all of those instructions.
Read rangers/coder.prompt, then read every file it refers to recursively, and follow all of those instructions.
